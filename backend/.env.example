# =============================================================================
# LexFlow Protocol - Backend Environment Variables
# =============================================================================
#
# IMPORTANT: This file contains example values for configuration.
# Copy this file to `.env` and set your actual values.
#
# For production deployment on Render:
# - Add these variables in the Render dashboard under Environment
# - Ensure sensitive values (API keys, secrets) are kept secure
#
# =============================================================================

# ===== Database Configuration =====
# PostgreSQL connection URL (provided by Render automatically)
# Format: postgresql://user:password@host:port/database
# Note: Render provides DATABASE_URL automatically, no manual config needed
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/lexflow

# ===== JWT Authentication =====
# Secret key for JWT token generation (MUST be changed in production!)
JWT_SECRET_KEY=your-super-secret-jwt-key-change-in-production-minimum-32-characters
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7

# ===== OpenAI API  =====
# Required for AI-powered contract analysis and obligation extraction
OPENAI_API_KEY=sk-...your-openai-api-key...

# ===== Frontend URL =====
# Used for CORS and email link generation
# Development: http://localhost:5173
# Production (Vercel): https://your-app.vercel.app
FRONTEND_URL=http://localhost:5173

# ===== CORS Origins =====
# Comma-separated list of allowed origins
# Update this to include your production frontend URL
CORS_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000,https://your-app.vercel.app

# ===== Logging Configuration =====
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
# Recommended: INFO for production, DEBUG for development
LOG_LEVEL=INFO

# JSON logging for production monitoring (true/false)
# Set to true for Render/cloud deployments
LOG_JSON=false

# ===== Email Notifications (Optional) =====
# Set USE_SMTP=true to enable actual email sending
USE_SMTP=false
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM_EMAIL=noreply@lexflow.example.com
SMTP_FROM_NAME=LexFlow Protocol

# ===== Blockchain Configuration (Optional) =====
# Ethereum RPC URL (e.g., Infura, Alchemy)
ETHEREUM_RPC_URL=https://sepolia.infura.io/v3/YOUR_INFURA_KEY

# Smart Contract Addresses (Sepolia Testnet)
ESCROW_CONTRACT_ADDRESS=0x...
JPYC_CONTRACT_ADDRESS=0x138D4810c6D977eE490f67D9659Cb26A89d630f9
TREASURY_ADDRESS=0x18c151c219b4B0B8D2E88B8c4F9A8b83596bf7Bb

# Private key for blockchain transactions (KEEP SECRET!)
PRIVATE_KEY=

# =============================================================================
# Production Deployment Notes (Render)
# =============================================================================
#
# 1. DATABASE_URL is automatically provided by Render when you add PostgreSQL
# 2. Set LOG_JSON=true for better structured logging in production
# 3. Ensure JWT_SECRET_KEY is a strong, random string
# 4. Add your Vercel frontend URL to CORS_ORIGINS
# 5. Set FRONTEND_URL to your production frontend URL
#
# =============================================================================
